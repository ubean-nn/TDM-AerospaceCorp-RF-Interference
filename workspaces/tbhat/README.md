# Tbhat Workspace
The hub for High-Performance Computing cluster configurations. Houses heavily structured SLURM submit scripts (sbatch), sweep architectures (weeper.py), and distributed PyTorch training jobs.
